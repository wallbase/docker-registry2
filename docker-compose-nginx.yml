nginx:
  container_name: nginx
  image: nginx
  restart: always
  volumes:
    - ./certs:/certs
    - ./nginx.conf:/etc/nginx/nginx.conf
  ports:
    - "80:80"
    - "443:443"
  links:
    - registry
    - docker-registry-ui
    - gotty
    - trackr-app-ui
    - trackr-web

